<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description"
          content="Rx Training Games - learn and practice Reactive Extensions coding grid-based games">
    <meta name="keywords" content="Rx, JavaScript, Reactive Extensions, Training, Playground, Games"/>

    <title>Rx Training Games - learn and practice Reactive Extensions coding grid-based games</title>

    <script src="js/lib/jailed/lib/jailed.js"></script><!-- https://github.com/asvd/jailed/issues/11-->

    <%
        // this file could benefit some https://github.com/tj/ejs#includes but it does not work with ejs-cli as render() requires the filename
        var appConfig = {
            appJS : devMode ? 'js/lib/requirejs/require.js' : 'dist/rx-training-games.app.min.js',
            runtimeJS : devMode ? 'runtime/main.js' : '../dist/rx-training-games.runtime.min.js',
            style: devMode ? 'style/style.css' : 'dist/style.min.css',
            devMode : devMode
        };

        var cacheBuster = '?cb=' + Math.floor(100000000000 + Math.random() * 900000000000);
        appConfig.appJS += cacheBuster;
        appConfig.runtimeJS += cacheBuster;
        appConfig.style += cacheBuster;
    %>

    <script>var appConfig = <%- JSON.stringify(appConfig) %>;</script>
    <script data-main="js/main.js" src="<%= appConfig.appJS %>"></script>
    <link rel="stylesheet" href="<%= appConfig.style %>">
    <link rel="icon" href="favicon.ico" />
</head>

<body>

<div class="container-fluid">
    <ng-view>
        Please wait while <a href="https://github.com/JulienMoumne/rx-training-games">Rx Training Games</a> is loading. <br/><br/>
        If this message does not disappear after a few seconds, your browser may not be compatible. <br/><br/>
        If your browser meets the <a href="https://github.com/JulienMoumne/rx-training-games#browser-compatibility">requirements</a>,
        please consider
        <a href="https://github.com/JulienMoumne/rx-training-games/issues/new">
            creating a GitHub issue
        </a>
        with your OS and browser details.
    </ng-view>
</div>

</body>

</html>